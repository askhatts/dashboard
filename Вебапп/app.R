# ============================================================
# ТОЧКА ВХОДА — Онкологическая аналитика Абайской области
# ============================================================
# Запуск: shiny::runApp(".")
# Или в RStudio: кнопка "Run App"
#
# Структура приложения:
#   global.R — пакеты, константы, начальные данные
#   ui.R     — пользовательский интерфейс (ArcGIS Dashboard стиль)
#   server.R — серверная логика, модули
#   R/       — модули и утилиты
#   www/     — CSS, JS (тёмная тема, fullscreen)
#   data/    — .rds файлы данных, шейпфайлы
#
# Первый запуск:
#   1. Выполните source("R/setup_database.R") для генерации .rds файлов
#   2. Затем shiny::runApp(".")
#
# Админ-панель:
#   Кликните на иконку gear в правом верхнем углу
#   Пароль по умолчанию: admin2024
# ============================================================

source("global.R")

shinyApp(
  ui     = source("ui.R", local = TRUE)$value,
  server = source("server.R", local = TRUE)$value
)
